import PhotoReleaseForm from "../components/photo-release-form/photo-release-form";
import "./photo-release.scss";

const PhotoRelease = () => {
    return (
        <div id="photo-release">
            <PhotoReleaseForm />
        </div>
    );
};

export default PhotoRelease;
