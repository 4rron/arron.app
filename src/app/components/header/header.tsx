import LightModeToggle from "../light-mode-toggle/light-mode-toggle";

export default function Header() {
    return (
        <div className="header">
            <LightModeToggle />
        </div>
    );
}
